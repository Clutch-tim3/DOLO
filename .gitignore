data/
__pycache__/
*.pyc
.env
venv/
env/
.pytest_cache/
.hypothesis/
.DS_Store
catboost_info/
*.log
*.sqlite
*.db
google-cloud-sdk
gcloud.zip

# Generated quotation PDFs — runtime output, not source
static/downloads/
firebase_public/downloads/

# Editor / tooling backups
*.bak

# .env alone does not match .env.local
.env.local
.env.*.local

# Credentials — access_token.txt is a live GCP token. It was untracked but not
# ignored, so a `git add .` would have published it. Cloud sessions commit and
# push, which makes that a real risk rather than a theoretical one.
access_token.txt
*.token
service-account*.json

# Local run noise
logs.txt
scratch_scripts/
